---
import Page from '@/layouts/Page.astro';
import { getDictionary, getLocalizedPath } from '@/i18n/index';
import { SUPPORTED_LANGS } from '@/i18n/types';
import type { Lang } from '@/i18n/types';

export function getStaticPaths() {
  return SUPPORTED_LANGS.map((lang) => ({ params: { lang } }));
}

const { lang } = Astro.params as { lang: Lang };
const t = await getDictionary(lang);
const p = t.privacy;
---

<Page
  lang={lang}
  t={t}
  title={t.meta.privacyTitle}
  description={t.meta.privacyDesc}
  canonicalPath="privacy"
>
  <section class="bg-slate-bg py-12 md:py-16 border-b border-slate-border">
    <div class="container-xl max-w-3xl">
      <span class="inline-block text-electric text-xs font-bold uppercase tracking-widest mb-3">
        {p.badge}
      </span>
      <h1 class="text-4xl md:text-5xl font-bold text-navy mb-4">{p.heading}</h1>
      <p class="text-slate-500 text-sm">{p.lastUpdated}</p>
    </div>
  </section>

  <section class="py-12 md:py-16 bg-white">
    <div class="container-xl max-w-3xl flex flex-col gap-10">
      {p.sections.map((section) => (
        <div class="flex flex-col gap-3">
          <h2 class="text-xl font-bold text-navy flex items-center gap-3">
            <span class="w-1 h-5 bg-electric rounded-full shrink-0" aria-hidden="true"></span>
            {section.heading}
          </h2>
          <p class="text-slate-600 leading-relaxed pl-4">{section.body}</p>
        </div>
      ))}

      <div class="bg-slate-bg rounded-2xl p-6 border border-slate-border">
        <p class="font-semibold text-navy text-sm mb-1">{p.contactHeading}</p>
        <p class="text-slate-600 text-sm leading-relaxed">{p.contactBody}</p>
        <a href="mailto:info@nextgenfinca.com" class="text-electric text-sm hover:underline font-medium mt-2 inline-block">
          info@nextgenfinca.com
        </a>
      </div>
    </div>
  </section>
</Page>
